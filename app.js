const steps=[...document.querySelectorAll('.step')];const totalSteps=steps.length;const progressText=document.querySelector('#progress-text');const progressBar=document.querySelector('#progress-bar');const progressTrack=document.querySelector('.progress-track');const previewPanel=document.querySelector('#preview-panel');const soundToggle=document.querySelector('#sound-toggle');const toast=document.querySelector('#toast');const lessonActivity=document.querySelector('#lesson-activity');const activityLabel=document.querySelector('#activity-label');const contentTitle=document.querySelector('#content-title');const contentInstruction=document.querySelector('#content-instruction');const activityBadge=document.querySelector('#activity-badge');const activityBody=document.querySelector('#activity-body');const activityFeedback=document.querySelector('#activity-feedback');const nextActivity=document.querySelector('#next-activity');
const warmUpQuestions=[{prompt:'Which word is related to a school?',options:['school','banana','river','bedroom'],answer:'school',explanation:'School means “trường học”, so it is the word directly related to the topic My New School.'},{prompt:'You study with your classmates in a …',options:['classroom','kitchen','garden','bathroom'],answer:'classroom',explanation:'A classroom is a room where students learn together. “Kitchen”, “garden” and “bathroom” are not normally places for a class.'},{prompt:'Which sentence is a natural way to introduce your new school?',options:['This is my new school.','This are my new school.','These is my new school.','This am my new school.'],answer:'This is my new school.',explanation:'We use “This is …” to introduce one thing. The other choices use the wrong form of be.'}];
let currentQuestion=0,score=0,answered=false;
function shuffleOptions(a){const s=[...a];for(let i=s.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[s[i],s[j]]=[s[j],s[i]]}return s}
function selectStep(n){const safe=Math.min(Math.max(n,1),totalSteps),p=Math.round(safe/totalSteps*100);steps.forEach(x=>x.classList.toggle('is-active',Number(x.dataset.step)===safe));progressText.textContent=`${safe} / ${totalSteps}`;progressBar.style.width=`${p}%`;progressTrack.setAttribute('aria-label',`Tiến độ ${p}%`);renderStep(safe)}
function renderWarmUp(){const q=warmUpQuestions[currentQuestion],opts=shuffleOptions(q.options);answered=false;activityLabel.textContent='KHỞI ĐỘNG • QUICK START';contentTitle.textContent='Let’s get started!';contentInstruction.textContent=q.prompt;activityBadge.textContent=`${currentQuestion+1} / ${warmUpQuestions.length}`;activityFeedback.textContent='';nextActivity.textContent=currentQuestion===warmUpQuestions.length-1?'Hoàn thành ✓':'Câu tiếp theo →';activityBody.innerHTML=`<div class="question-card"><div class="question-number">Câu ${currentQuestion+1}</div><div class="question-prompt">${q.prompt}</div><div class="answer-grid">${opts.map((o,i)=>`<button class="answer-option" type="button" data-answer="${o}"><span>${String.fromCharCode(65+i)}</span>${o}</button>`).join('')}</div></div>`;activityBody.querySelectorAll('.answer-option').forEach(b=>b.addEventListener('click',()=>checkAnswer(b.dataset.answer)))}
function checkAnswer(a){if(answered)return;answered=true;const q=warmUpQuestions[currentQuestion],buttons=[...activityBody.querySelectorAll('.answer-option')],chosen=buttons.find(b=>b.dataset.answer===a),correct=a===q.answer;buttons.forEach(b=>{b.disabled=true;if(b.dataset.answer===q.answer)b.classList.add('is-correct')});if(chosen&&!correct)chosen.classList.add('is-wrong');if(correct){score++;activityFeedback.innerHTML=`<strong class="feedback-good">✓ Chính xác!</strong> ${q.explanation}`}else activityFeedback.innerHTML=`<strong class="feedback-bad">✗ Chưa đúng.</strong> Đáp án đúng là <b>${q.answer}</b>. ${q.explanation}`}
function showFinalWarmUp(){activityLabel.textContent='KHỞI ĐỘNG • HOÀN THÀNH';contentTitle.textContent='Tuyệt vời! Em đã khởi động xong.';contentInstruction.textContent='Hãy xem kết quả trước khi chuyển sang phần Từ vựng.';activityBadge.textContent='✓';activityBody.innerHTML=`<div class="result-card"><div class="result-score">${score}<span>/${warmUpQuestions.length}</span></div><div><h3>Kết quả khởi động</h3><p>${score===warmUpQuestions.length?'Rất tốt! Em đã sẵn sàng học Unit 1.':'Em hãy xem lại phần giải thích rồi tiếp tục nhé.'}</p></div></div>`;activityFeedback.textContent='';nextActivity.textContent='Sang Từ vựng →'}
function renderPlaceholder(n){const names=['Khởi động','Từ vựng','Ngữ pháp','Luyện tập','Đọc hiểu','Nói','Thử thách'],desc=['Làm quen với chủ đề My New School.','Phần từ vựng sẽ gồm từ mới, IPA, nghĩa tiếng Việt và phát âm.','Phần ngữ pháp sẽ trình bày lý thuyết, ví dụ và bài tập có giải thích.','Phần luyện tập sẽ có các dạng bài tương tác và chấm điểm.','Phần đọc hiểu sẽ có bài đọc, câu hỏi và kiểm tra mức độ hiểu bài.','Phần nói sẽ cung cấp từ gợi ý, mẫu câu và nhiệm vụ Speaking.','Phần thử thách sẽ tổng hợp kiến thức của Unit 1.'];activityLabel.textContent=`PHẦN ${String(n).padStart(2,'0')}`;contentTitle.textContent=names[n-1];contentInstruction.textContent=desc[n-1];activityBadge.textContent=`${n} / ${totalSteps}`;activityBody.innerHTML='<div class="coming-card"><span class="coming-icon">✦</span><div><h3>Nội dung đang được xây dựng</h3><p>Khung tương tác của phần này đã sẵn sàng. Chúng ta sẽ đưa nội dung Unit 1 vào từng bước tiếp theo.</p></div></div>';activityFeedback.textContent='';nextActivity.textContent=n<totalSteps?'Sang phần tiếp theo →':'Hoàn tất Unit 1'}
function renderStep(n){if(n===1){previewPanel.classList.add('show');renderWarmUp()}else{previewPanel.classList.remove('show');renderPlaceholder(n)}}
steps.forEach(s=>s.addEventListener('click',()=>{selectStep(Number(s.dataset.step));lessonActivity.scrollIntoView({behavior:'smooth',block:'center'})}));document.querySelector('#start-activity').addEventListener('click',()=>{currentQuestion=0;score=0;selectStep(1);lessonActivity.scrollIntoView({behavior:'smooth',block:'center'})});nextActivity.addEventListener('click',()=>{if(currentQuestion<warmUpQuestions.length-1&&document.querySelector('.answer-option')){if(!answered){activityFeedback.innerHTML='<strong class="feedback-bad">Hãy chọn một đáp án trước nhé.</strong>';return}currentQuestion++;renderWarmUp();return}if(currentQuestion===warmUpQuestions.length-1&&answered){showFinalWarmUp();return}if(currentQuestion===warmUpQuestions.length-1&&!document.querySelector('.answer-option')){selectStep(2);lessonActivity.scrollIntoView({behavior:'smooth',block:'center'})}});soundToggle.addEventListener('click',()=>{const enabled=soundToggle.getAttribute('aria-pressed')!=='true';soundToggle.setAttribute('aria-pressed',String(enabled));toast.textContent=enabled?'Đã bật âm thanh hướng dẫn!':'Đã tắt âm thanh hướng dẫn!';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2600)});renderStep(1);
